#pragma once

/*
 * Copy this file to local_credentials.h and fill in your local values.
 * local_credentials.h is ignored by Git. Never commit or share it.
 * The values are compiled into the firmware and then copied to NVS at boot.
 */

#include "config_store.h"

static const app_config_t minitv_local_config = {
    .wifi_ssid = "YOUR_2G_WIFI_SSID",
    .wifi_password = "YOUR_WIFI_PASSWORD",
    .ha_endpoint = "http://192.169.0.28:8123",
    .ha_token = "YOUR_HOME_ASSISTANT_LONG_LIVED_TOKEN",
    .weather_entity = "weather.forecast_home",
    .brightness_percent = 55,
};
